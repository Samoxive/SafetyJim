CREATE TABLE BanList (
    BannedUserID      TEXT    PRIMARY KEY ON CONFLICT FAIL,
    BannedUserName    TEXT,
    ModeratorID       TEXT,
    ModeratorUserName TEXT,
    GuildID           TEXT,
    BanTime           INTEGER,
    ExpireTime        INTEGER,
    Reason            TEXT,
    Expires           BOOLEAN);